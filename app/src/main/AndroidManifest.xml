<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.timbo.kartoffel">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="de.timbo.kartoffel.BaseApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher_potato"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_potato_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="de.timbo.kartoffel.ui.select.SelectActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="de.timbo.kartoffel.ui.setup.SetupActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="de.timbo.kartoffel.ui.recipes.RecipesActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="de.timbo.kartoffel.ui.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="de.timbo.kartoffel.android.GenericFileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>